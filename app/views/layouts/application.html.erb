<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/head_setting' %>
    <%= render 'layouts/shim' %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
